[flavor]
name = "qren-coder"
version = "1.1.0"
license = "AGPL-3.0-or-later"
entrypoint = "vanilla_flavor:run"
capabilities = [
  # QRCF container format
  "encode", "decode", "verify", "block-types", "self-test",
  # semantic layer
  "classify", "slime-phase", "tokens", "circle",
]
